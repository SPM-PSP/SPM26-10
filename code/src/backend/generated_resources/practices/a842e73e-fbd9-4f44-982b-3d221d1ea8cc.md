---

题目1: [选择题] 使用TensorFlow Lite进行花卉识别时，需要将预训练的MobileNet V2模型冻结哪些部分？  
参考答案1: 顶层权重  
[编程题:  
```python  
import tensorflow as tf  
# 导入预训练模型  
model = tf.keras.models.load_model('pretrained_model.h5')  
# 具体代码根据模型结构进行冻结  
model.trainable = False  
# 训练模型  
model.compile(optimizer='adam', loss='categorical_crossentropy')  
```  
代码解释：冻结预训练模型的顶层权重，仅更新分类器权重。]  

---

题目2: [填空题] MobileNet V2模型的输入尺寸通常为 ____.  
参考答案2: 224x224  
[编程题:  
```python  
# 预处理输入  
image = tf.io.read_file('flower.jpg')  
image = tf.image.resize_images(image, [224, 224])  
# 应用TFLite转换器  
converter = tf.lite.Converter.from_network(model)  
quant_model = converter.optimize()  
quant_model.save('flower.tflite')  
```  
代码解释：使用TFLite转换器将模型转换为二进制文件，输入尺寸设置为224x224以适配Android设备。]