# 类对象与封装继承多态

## 1. 类与对象
类是对象的抽象模板，对象是类的实例。类定义了对象的状态和行为。

## 2. 封装
封装要求把数据和对数据的操作封装到类内部，并通过访问控制限制外部直接访问。

## 3. 继承
继承使子类能够复用父类代码，并在其基础上扩展功能。学习时要重点区分：
- 单继承
- 多继承
- 接口继承
- 实现继承

## 4. 多态
多态让同一消息在不同对象上产生不同实现，是提升系统扩展能力的重要机制。
